<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }
        .container {
            background: white;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            padding: 30px;
            margin-top: 30px;
            margin-bottom: 30px;
            max-width: 600px;
        }
        .btn-primary {
            background: linear-gradient(45deg, #667eea, #764ba2);
            border: none;
            border-radius: 25px;
            padding: 12px 30px;
        }
        .btn-primary:hover {
            background: linear-gradient(45deg, #5a6fd8, #6a4190);
        }
        .form-control, .form-select {
            border-radius: 10px;
            border: 2px solid #e9ecef;
            padding: 12px 15px;
        }
        .form-control:focus, .form-select:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.25);
        }
        .card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }
        .alert {
            border-radius: 10px;
        }
        .payment-option {
            border: 2px solid #e9ecef;
            border-radius: 10px;
            padding: 15px;
            margin-bottom: 10px;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        .payment-option:hover {
            border-color: #667eea;
            background-color: #f8f9fa;
        }
        .payment-option.selected {
            border-color: #667eea;
            background-color: #e7f3ff;
        }
        .payment-icon {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 18px;
            margin-right: 15px;
        }
        .credit-card { background: linear-gradient(45deg, #667eea, #764ba2); }
        .cash { background: linear-gradient(45deg, #28a745, #20c997); }
        .transfer { background: linear-gradient(45deg, #ffc107, #fd7e14); }
    </style>
</head>
<body>
    <div class="container">
        <div class="text-center mb-4">
            <h1 class="mb-2"><i class="fas fa-credit-card text-primary me-2"></i><%= title %></h1>
            <p class="text-muted">Agrega un nuevo método de pago al sistema</p>
        </div>

        <div class="card">
            <div class="card-body">
                <% if (typeof error !== 'undefined' && error) { %>
                    <div class="alert alert-danger" role="alert">
                        <i class="fas fa-exclamation-triangle me-2"></i>
                        <%= error %>
                    </div>
                <% } %>

                <form action="/metodospago" method="POST">
                    <div class="mb-3">
                        <label for="nombre" class="form-label">
                            <i class="fas fa-tag me-1"></i>Nombre del Método de Pago *
                        </label>
                        <input type="text" class="form-control" id="nombre" name="nombre"
                               value="<%= typeof metodo !== 'undefined' ? metodo.nombre || '' : '' %>"
                               placeholder="Ej: Tarjeta de Crédito, Efectivo, Transferencia Bancaria" required>
                        <div class="form-text">Ingresa el nombre completo del método de pago</div>
                    </div>

                    <div class="mb-4">
                        <label class="form-label">
                            <i class="fas fa-mouse-pointer me-1"></i>Sugerencias de Métodos de Pago
                        </label>
                        <div class="row">
                            <div class="col-md-6">
                                <div class="payment-option" onclick="selectPaymentOption('Tarjeta de Crédito')">
                                    <div class="d-flex align-items-center">
                                        <div class="payment-icon credit-card">
                                            <i class="fas fa-credit-card"></i>
                                        </div>
                                        <div>
                                            <strong>Tarjeta de Crédito</strong>
                                            <br><small class="text-muted">Visa, Mastercard, etc.</small>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="payment-option" onclick="selectPaymentOption('Efectivo')">
                                    <div class="d-flex align-items-center">
                                        <div class="payment-icon cash">
                                            <i class="fas fa-money-bill-wave"></i>
                                        </div>
                                        <div>
                                            <strong>Efectivo</strong>
                                            <br><small class="text-muted">Pago en caja</small>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="payment-option" onclick="selectPaymentOption('Transferencia Bancaria')">
                                    <div class="d-flex align-items-center">
                                        <div class="payment-icon transfer">
                                            <i class="fas fa-university"></i>
                                        </div>
                                        <div>
                                            <strong>Transferencia Bancaria</strong>
                                            <br><small class="text-muted">Depósito bancario</small>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="payment-option" onclick="selectPaymentOption('Tarjeta de Débito')">
                                    <div class="d-flex align-items-center">
                                        <div class="payment-icon credit-card">
                                            <i class="fas fa-credit-card"></i>
                                        </div>
                                        <div>
                                            <strong>Tarjeta de Débito</strong>
                                            <br><small class="text-muted">Débito automático</small>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                        <a href="/metodospago" class="btn btn-outline-secondary me-md-2">
                            <i class="fas fa-times me-1"></i>Cancelar
                        </a>
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-save me-1"></i>Guardar Método
                        </button>
                    </div>
                </form>
            </div>
        </div>

        <div class="text-center mt-3">
            <small class="text-muted">* Campos obligatorios</small>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        function selectPaymentOption(optionName) {
            document.getElementById('nombre').value = optionName;

            // Remove selected class from all options
            document.querySelectorAll('.payment-option').forEach(option => {
                option.classList.remove('selected');
            });

            // Add selected class to clicked option
            event.currentTarget.classList.add('selected');
        }

        // Auto-focus on input field
        document.addEventListener('DOMContentLoaded', function() {
            document.getElementById('nombre').focus();
        });
    </script>
</body>
</html>